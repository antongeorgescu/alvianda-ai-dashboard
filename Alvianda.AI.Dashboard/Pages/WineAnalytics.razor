@page "/wineanalytics"
@*@implements IDisposable*@

@*<div class="form-group">
        <table style="padding:5px;">
            <thead>
                <tr>
                    <td><label>Chat Service - use Analytics Controller with Signal-R</label></td>
                </tr>
            </thead>
            <tr>
                <td>
                    <label>
                        User:
                        <input @bind="userInput" />
                    </label>
                </td>
                <td>
                    <label>
                        Message:
                        <input @bind="messageInput" size="50" />
                    </label>
                </td>
                <td><button @onclick="Connect">Connect Chat Service</button></td>
                <td><button @onclick="Send">Send Message</button></td>
            </tr>
        </table>
    </div>*@
<h3>Wine Analytics with Machine Learning: Training and Fitting Algorithms</h3>
<table class="table" style='font-size:small;line-height: 1.428;'>
    <thead style="background-color:gainsboro; border-color:gray;">
        <tr>
            <td>
                <div>
                    <label for="start">Select algorithm:</label>
                    <select @bind="@SelectedAlgorithm" class="select-box">
                        <option value="None">== None ==</option>
                        <option value="decision-tree">Decision Tree</option>
                        <option value="naive-bayes">Naive Bayes</option>
                    </select>
                </div>
            </td>
            <td class="text-left">
                <button class="btn btn-primary" @onclick="@RunMachineLearningAnalysis" style='font-size:small;line-height: 1.428;width:205px;'>Run Machine Learning Analysis</button>
            </td>
        </tr>
    </thead>
</table>
<hr />

<div>
    <label>Session Summary:</label>
    <ul id="messagesList">
        @foreach (var message in messages)
        {
            if (message.Item1 == "error")
            {
                <li style="color:red">[@message.Item1] @message.Item2</li>
            }
            if (message.Item1 == "info")
            {
                <li style="color:green">[@message.Item1] @message.Item2</li>
            }
        }
    </ul>
</div>

<p><i><mark>@waitMessage</mark></i></p>

@if (isRunDataAvailable)
{
<table border="3">
    <tr>
        <td><b>Chart: @attributesHistogramTitle</b></td>
        <td>@qualityHistogramTitle</td>
    </tr>
    <tr>
        <td><img src=@attributesHistogramChart width="100%" height="400" /></td>
        <td><img src=@qualityHistogramChart /></td>
    </tr>
    <tr>
        <td><b>Correlation Attributes:</b> @correlationAttributes</td>
        <td><b>Quality Values Dropped:</b> @qualityValuesDropped</td>
    </tr>
    <tr>
        <td><b>Chart: @correlationTitle</b></td>
        <td>&nbsp;</td>
    </tr>
    <tr>
        <td><img src=@correlationChart height="100%"/></td>
        <td>&nbsp;</td>    
    </tr>

</table>
}

<!--<img src="http://localhost:53535/static/John-Snow.jpg" />-->
@code {
}